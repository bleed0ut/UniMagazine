@model IEnumerable<Faculty>
@{
    Layout = "/Areas/Admin/Views/_LayoutAdminPage.cshtml";
    ViewData["Title"] = "Admin page";
}

<div class="d-flex flex-row-reverse">
    <a asp-area="Admin" asp-controller="Faculty" asp-action="Add" class="btn text-white" style="background-color: var(--secondary-color)">
        <span class="btn-label">
            <i class="fa-solid fa-plus"></i>
        </span>   Add New Faculty
    </a>
</div>
<div class="text-center shadow mt-2">
    <div class="bg-secondary bg-gradient py-2 text-white text-center">
        <h3>Available faculties - Admin mamangement</h3>
    </div>
    <div class="container py-3">
            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Desription</th>
                        <th>Created Date</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var fal in Model)
                    {
                        <tr>
                            <td>@fal.Id</td>
                            <td>@fal.Name</td>
                            <td>@fal.Description</td>
                            <td>@fal.CreateDate</td>
                            <td>
                                <a asp-area="Admin"
                                   asp-action="Update"
                               asp-route-id="@fal.Id" class="btn btn-secondary">Detail</a>
                            </td> 
                        </tr>
                    }
                </tbody>
            </table>
    </div>
</div>